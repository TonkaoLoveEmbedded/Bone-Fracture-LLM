export const LLM_CONFIG = {
  baseUrl: process.env.OLLAMA_BASE_URL || "http://localhost:11434",
  model: process.env.LLM_MODEL || "scb10x/llama3.2-typhoon2-1b-instruct",
  options: {
    temperature: 0.3,  // ต่ำไว้ ลดการแต่งเรื่อง (สำคัญมากในงานคลินิก)
    top_p: 0.9,
    num_ctx: 4096,
  },
};

// system prompt ต้องตอกย้ำว่า "ผู้ช่วยอธิบาย" ไม่ใช่ "ผู้วินิจฉัย"
export const SYSTEM_PROMPT = `คุณเป็นผู้ช่วยอธิบายผลการวิเคราะห์ภาพเอกซเรย์สำหรับบุคลากรทางการแพทย์
หน้าที่ของคุณคืออธิบายและตอบคำถามโดยอ้างอิงเฉพาะ "บริบทผลวิเคราะห์" ที่ระบบส่งให้เท่านั้น

กฎสำคัญ:
- ห้ามวินิจฉัยเอง ห้ามสร้างข้อมูลที่ไม่มีในบริบท หากไม่มีข้อมูลให้ตอบว่าไม่ทราบ
- ย้ำเสมอว่าผลนี้เป็นเครื่องมือช่วยตัดสินใจ ต้องให้แพทย์ตรวจทานและลงนามยืนยัน
- ตอบเป็นภาษาเดียวกับคำถามของผู้ใช้ กระชับ ตรงประเด็น`;